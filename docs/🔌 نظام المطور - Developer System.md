# 🔌 نظام المطور - Developer System

> العمود الفقري للتكامل بين جميع أنظمة إدارة الكهرباء

[![NestJS](https://img.shields.io/badge/NestJS-11.x-red.svg)](https://nestjs.com/)
[![Angular](https://img.shields.io/badge/Angular-21.x-red.svg)](https://angular.io/)
[![Prisma](https://img.shields.io/badge/Prisma-5.x-blue.svg)](https://prisma.io/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.9-blue.svg)](https://www.typescriptlang.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

---

## 📋 نظرة عامة

نظام المطور هو النظام الثاني من أصل 10 أنظمة في منظومة إدارة الكهرباء. يوفر:

- **بوابة API موحدة** للتواصل بين جميع الأنظمة
- **نظام أحداث** لنشر واستقبال الأحداث بين الأنظمة
- **تكاملات خارجية** مع بوابات الدفع وخدمات الرسائل وأجهزة IoT
- **وحدة ذكاء اصطناعي** للتحليل والتنبؤ
- **أدوات مراقبة** لصحة الأنظمة والتنبيهات

---

## 🏗️ الأنظمة المتكاملة

| # | النظام | الرمز | الوصف |
|---|--------|-------|-------|
| 1 | النظام الأم | core | إدارة العملاء والعدادات |
| 2 | نظام الأصول | assets | إدارة المحولات والشبكات |
| 3 | العمليات الميدانية | field | إدارة الفرق والمهام |
| 4 | نظام المراقبة | scada | مراقبة الشبكة في الوقت الفعلي |
| 5 | نظام المخزون | inventory | إدارة المواد والمستودعات |
| 6 | نظام الفوترة | billing | الفواتير والتحصيل |
| 7 | الموارد البشرية | hr | إدارة الموظفين |
| 8 | نظام التقارير | reports | التقارير والتحليلات |
| 9 | نظام المشاريع | projects | إدارة المشاريع |

---

## 🚀 التثبيت والتشغيل

### المتطلبات
- Node.js 22.x
- PostgreSQL 14+
- pnpm 9.x

### خطوات التثبيت

```bash
# استنساخ المستودع
git clone https://github.com/alabasi2025/02-developer-system.git
cd 02-developer-system

# تثبيت الاعتماديات
pnpm install

# إعداد قاعدة البيانات
cp .env.example .env
# تعديل DATABASE_URL في ملف .env

# تشغيل migrations
npx prisma db push
npx prisma generate

# بناء المشروع
npx nx build api
npx nx build web

# تشغيل الخوادم
node dist/apps/api/main.js &
npx serve -s dist/apps/web/browser -l 4200
```

### روابط الوصول
- **Backend API:** http://localhost:3000/api/v1
- **Swagger Docs:** http://localhost:3000/docs
- **Frontend:** http://localhost:4200

---

## 📁 هيكل المشروع

```
02-developer-system/
├── apps/
│   ├── api/                 # Backend NestJS
│   │   └── src/
│   │       ├── app/         # App Module
│   │       ├── modules/     # Feature Modules
│   │       │   ├── integrations/
│   │       │   ├── api-keys/
│   │       │   ├── events/
│   │       │   ├── gateway/
│   │       │   ├── monitoring/
│   │       │   ├── payments/
│   │       │   ├── messages/
│   │       │   ├── iot/
│   │       │   ├── ai/
│   │       │   ├── internal-api/
│   │       │   └── external-api/
│   │       └── prisma/      # Prisma Service
│   └── web/                 # Frontend Angular
│       └── src/
│           └── app/
│               ├── core/    # Services & Models
│               ├── features/# Feature Components
│               └── layouts/ # Layout Components
├── prisma/
│   └── schema.prisma        # Database Schema
├── docs/                    # Documentation
└── package.json
```

---

## 🔧 الوحدات (Modules)

### 1. التكاملات (Integrations)
إدارة التكاملات الداخلية والخارجية مع الأنظمة الأخرى.

### 2. مفاتيح API (API Keys)
إنشاء وإدارة مفاتيح الوصول مع الصلاحيات والحدود.

### 3. الأحداث (Events)
نظام pub/sub للتواصل غير المتزامن بين الأنظمة.

### 4. البوابة (Gateway)
توجيه الطلبات بين الأنظمة مع Rate Limiting.

### 5. المراقبة (Monitoring)
فحص صحة الأنظمة، المقاييس، السجلات، والتنبيهات.

### 6. المدفوعات (Payments)
تكامل مع بوابات الدفع (STC Pay, Mada, Stripe).

### 7. الرسائل (Messages)
إرسال SMS, Email, WhatsApp, Push Notifications.

### 8. أجهزة IoT
إدارة العدادات الذكية وأجهزة القياس.

### 9. الذكاء الاصطناعي (AI)
تحليل البيانات، التنبؤ، المحادثة الذكية.

---

## 📊 قاعدة البيانات

### الجداول الرئيسية
| الجدول | الوصف |
|--------|-------|
| `dev_integrations` | التكاملات |
| `dev_api_keys` | مفاتيح API |
| `dev_events` | الأحداث |
| `dev_event_subscriptions` | اشتراكات الأحداث |
| `dev_payment_gateways` | بوابات الدفع |
| `dev_payment_transactions` | معاملات الدفع |
| `dev_message_providers` | مزودي الرسائل |
| `dev_message_templates` | قوالب الرسائل |
| `dev_messages` | الرسائل |
| `dev_iot_devices` | أجهزة IoT |
| `dev_iot_data` | بيانات IoT |
| `dev_iot_commands` | أوامر IoT |
| `dev_alerts` | التنبيهات |
| `dev_metrics` | المقاييس |
| `dev_logs` | السجلات |
| `dev_ai_requests` | طلبات AI |

---

## 🔐 الأمان

- **المصادقة:** API Keys مع تشفير SHA-256
- **التفويض:** نظام صلاحيات مرن
- **Rate Limiting:** حماية من الإساءة
- **IP Whitelist:** تقييد الوصول بالـ IP
- **Audit Logging:** تسجيل جميع العمليات

---

## 📈 نقاط النهاية الرئيسية

| المسار | الوصف |
|--------|-------|
| `GET /api/v1/monitoring/health` | فحص صحة الأنظمة |
| `POST /api/v1/integrations` | إنشاء تكامل جديد |
| `POST /api/v1/api-keys` | إنشاء مفتاح API |
| `POST /api/v1/events` | نشر حدث |
| `GET /api/v1/events/subscriptions` | قائمة الاشتراكات |
| `POST /api/v1/gateway/route` | توجيه طلب |
| `POST /api/v1/payments/process` | معالجة دفعة |
| `POST /api/v1/messages/send` | إرسال رسالة |
| `POST /api/v1/iot/data` | استقبال بيانات IoT |
| `POST /api/v1/ai/analyze` | تحليل بيانات |
| `POST /api/v1/ai/predict` | التنبؤ |
| `POST /api/v1/ai/chat` | المحادثة الذكية |

---

## 🧪 الاختبارات

```bash
# Unit Tests
npx nx test api

# E2E Tests
npx nx e2e api-e2e

# Coverage
npx nx test api --coverage
```

---

## 📚 التوثيق

- [توثيق API](docs/API_DOCUMENTATION.md)
- [خطة البناء](BUILD_PHASES.md)
- [القواعد الصارمة](docs/12_القواعد_الصارمة.md)
- [نظام المطور](docs/02_نظام_المطور.md)

---

## 🤝 المساهمة

1. Fork المستودع
2. إنشاء فرع جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push للفرع (`git push origin feature/amazing-feature`)
5. فتح Pull Request

---

## 📄 الترخيص

هذا المشروع مرخص تحت رخصة MIT - انظر ملف [LICENSE](LICENSE) للتفاصيل.

---

## 📞 الدعم

للمساعدة التقنية:
- 📧 Email: support@electricity.sa
- 📱 Phone: 920000000

---

<div align="center">
  <p>صُنع بـ ❤️ لمنظومة إدارة الكهرباء</p>
</div>
